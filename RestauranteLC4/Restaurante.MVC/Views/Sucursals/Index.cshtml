@model IEnumerable<Restaurante.Entities.Sucursal>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Encuesta.Resultado)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.OrdenCompra.Insumo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DireccionId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Encuesta.Resultado)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.OrdenCompra.Insumo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DireccionId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SucursalId }) |
            @Html.ActionLink("Details", "Details", new { id=item.SucursalId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SucursalId })
        </td>
    </tr>
}

</table>
