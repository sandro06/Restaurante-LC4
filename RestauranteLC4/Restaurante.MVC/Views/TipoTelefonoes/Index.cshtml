@model IEnumerable<Restaurante.Entities.TipoTelefono>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Cliente.ApeMat)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Numero)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Cliente.ApeMat)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Numero)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TipoTelefonoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TipoTelefonoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TipoTelefonoId })
        </td>
    </tr>
}

</table>
